package uniformDistribution;

import java.util.Random;
import java.util.Scanner;

public class UniformDistribution {
	public static void main(String args[]) {

		// Declare an array of length 10
		String[] numberContainer = new String[10];

		// Get the number of values from the user and store in 'numberOfValues'
		Scanner scanner = new Scanner(System.in);
		System.out.print("How many values would you like to test? ");
		int numberOfValues = scanner.nextInt();

		// This line of code is used during testing
		// int numberOfValues = 100;

		// Populate the array with an empty String in each index
		initializeArray(numberContainer);

		// This will load the array 'numberContainer' with 'numberOfValues'
		// worth of data (so if 'numberOfValues' is 100, then 100 numbers
		// are randomly generated and put into the array in the appropriate cell
		loadArrayWithValues(numberContainer, numberOfValues);

		// This will output the values of 'numberContainer' into an appropriate
		// visual display
		displayMetrics(numberContainer);

	}
	
	/*
	 * This method takes in the array of String variables, 'numberContainer', and walks
	 * through each element, setting every value to the empty String (that is, "")
	 * 
	 */
	static void initializeArray(String[] numberContainer) {
		
		System.out.println("initializeArray() has been called");
		// This code exists only for debugging - it should be deleted or
		// commented out
	}
	
	
	/*
	 * This method will take in two parameters - an array of Strings
	 * ('numberContainer') and an int ('numberOfValues'). When invoked, this
	 * method will:
	 * 
	 * 1. Generate a random number between 0 and 9
	 * 
	 * 2. Append a "-" into the String housed in the appropriate array element.
	 * For instance, if the number '8' is generated, and the String in element 8
	 * (as referenced by 'numberContainer[8]') is "---", then the new value
	 * should be "----"
	 * 
	 * 3. This should happen repeatedly for 'numberOfValues' times. For
	 * instance, if 'numberOfValues' is 75, then this method should generate 75
	 * random numbers between 0 and 9 and adjust the corresponding Strings
	 * appropriately
	 */
	static void loadArrayWithValues(String[] numberContainer, int numberOfValues) {

		System.out.println("loadArrayWithValues() has been called");
		// This code exists only for debugging - it should be deleted or
		// commented out
		
	}

	/*
	 * This method should walk through the array 'numberContainer', and output
	 * two things:
	 * 
	 * 1. The index number of the element with a colon and a space after it, and
	 * 
	 * 2. The contents of the element (for instance, "-------")
	 */
	static void displayMetrics(String[] numberContainer) {
		
		System.out.println("displayMetrics() has been called");
		// This code exists only for debugging - it should be deleted or
		// commented out
		
	}

}
